steps:
- id: 'download data'
  uses: './pytorch'
  runs: './scripts/download_dataset.sh'

- id: 'verify data'
  uses: './pytorch'
  runs: './scripts/verify_dataset.sh'

- id: 'run benchmark'
  uses: './pytorch'
  runs: './scripts/run_benchmark.sh'
