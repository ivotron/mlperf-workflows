steps:
- id: 'download data'
  uses: 'docker://bizongroup/alpine-curl-bash'
  runs: './scripts/download_dataset.sh'

- id: 'verify data'
  uses: 'docker://frolvlad/alpine-bash'
  runs: './scripts/verify_dataset.sh'

- id: 'run benchmark'
  uses: './pytorch'
  runs: './scripts/run_benchmark.sh'
